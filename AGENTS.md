# AGENTS.md

## Cursor Cloud specific instructions

This repository (YUZU_CLEAN) is currently a blank project with only a `README.md`. There are no services, dependencies, build systems, or source code to run.

- **No package manager lockfiles** exist — no npm/yarn/pnpm/pip/cargo dependencies to install.
- **No Docker or docker-compose** configuration exists.
- **No lint, test, or build commands** are available yet.
- **No dev server or application** to start.

When source code and a build system are added, update this section with:
1. How to install dependencies (package manager + command).
2. How to run lint, tests, and the dev server.
3. Any non-obvious environment setup caveats.
